<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grey">

    <ImageView
        android:id="@+id/ivRedCircle"
        android:layout_width="@dimen/image_size"
        android:layout_height="@dimen/image_size"
        android:layout_above="@id/anchor"
        android:layout_margin="@dimen/horiz_margin"
        android:layout_toStartOf="@id/anchor"
        android:layout_toLeftOf="@id/anchor"
        android:background="@drawable/red_circle" />

    <ImageView
        android:id="@+id/ivBlueLine"
        android:layout_width="@dimen/image_size"
        android:layout_height="@dimen/image_size"
        android:layout_above="@id/anchor"
        android:layout_margin="@dimen/horiz_margin"
        android:layout_toEndOf="@id/anchor"
        android:layout_toRightOf="@id/anchor"
        android:background="@drawable/blue_line" />

    <View
        android:id="@+id/anchor"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_centerInParent="true" />

    <ImageView
        android:layout_width="@dimen/image_size"
        android:layout_height="@dimen/image_size"
        android:id="@+id/ivGreenDroid"
        android:layout_below="@id/anchor"
        android:background="@drawable/ic_android_black_24dp"
        android:layout_toLeftOf="@id/anchor"
        android:layout_toStartOf="@id/anchor"
        android:layout_margin="@dimen/horiz_margin"
        android:transitionName="greenDroid"/>

    <ImageView
        android:layout_width="@dimen/image_size"
        android:layout_height="@dimen/image_size"
        android:id="@+id/ivYellowSquare"
        android:layout_below="@id/anchor"
        android:layout_toRightOf="@id/anchor"
        android:layout_toEndOf="@id/anchor"
        android:background="@drawable/yellow_square"
        android:layout_margin="@dimen/horiz_margin"/>

</RelativeLayout>